import { makeStyles } from "@material-ui/core";

const HeroProductsStyle = makeStyles({
    outerContainer: {
        backgroundColor: "rgb(247, 248, 249)",
        paddingTop: 90
    },
})

export default HeroProductsStyle